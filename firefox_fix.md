# TODO
Mention in the README, below the installation script:
- that if you are on fedora: you most likely will have to do the CDI fix (in the troubleshooting section)
- if you are using firefox, most likely you should do this in about:config:
media.webrtc.camera.allow-pipewire set to false
use the [#TIP] / [#NOTE] or whatever kind of github style markdown tag you find appropriate.

+ also remove the reset_environment from the install script
